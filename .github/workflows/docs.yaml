name: Algorithm Docs Generation

permissions:
  contents: read
  pull-requests: read

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.repository }}-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generate-docs:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - uses: ./.github/actions/setup-uv-project

      - name: Run generation script
        run: python docs/utils/gen_docs.py

      - name: Move generated file to correct directory
        run: mv compression.rst docs/user_manual/compression.rst

      - name: Verify generated file exists
        run: |
          if [ ! -f docs/user_manual/compression.rst ]; then
            echo "Error: Generated file 'docs/user_manual/compression.rst' not found!"
            exit 1
          fi

      - name: Upload generated docs
        uses: actions/upload-artifact@v4
        with:
          name: pruna-docs
          path: docs/user_manual/compression.rst
